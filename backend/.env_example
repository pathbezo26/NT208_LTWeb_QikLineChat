# Cổng chạy server
PORT=5000
 
# Chuỗi kết nối MongoDB Atlas
MONGO_URI=<Thay bằng link Mongo của bạn>
# Khóa bí mật để ký JWT — đặt chuỗi ngẫu nhiên dài, KHÔNG chia sẻ công khai
JWT_SECRET=qikline_super_secret_jwt_key_2026
 
# Thời gian hết hạn token
JWT_EXPIRES_IN=7d
 